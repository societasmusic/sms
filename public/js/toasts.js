// const toastElList = document.querySelectorAll('.toast');
// const toastList = [...toastElList].map(toastEl => new bootstrap.Toast(toastEl, {autohide:false}));

// const myToast = new bootstrap.Toast(".toast", {autohide:false});

// myToast.show();